name: Manual Build and Release

on:
  workflow_dispatch:
    inputs:
      version:       # 输入参数名
        description: 'Release version (e.g. v1.5)' # 提示
        required: false
        default: ''   # 默认空，自动生成日期版本

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      # 1️⃣ 检出源码
      - name: Checkout source
        uses: actions/checkout@v4

      # 2️⃣ 设置 JDK 21 (对应 pom.xml 配置)
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      # 3️⃣ 构建 (跳过测试以加快速度)
      - name: Build with Maven
        run: mvn clean package -DskipTests

      # 4️⃣ 设置 Release 版本号标签
      - name: Set version tag
        id: version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            # 默认使用日期作为 Release 标签，例如 v24.5.20
            echo "version=v$(date +'%y.%-m.%-d')" >> $GITHUB_OUTPUT
          fi

      # 5️⃣ 创建 Release 并上传 jar
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: "${{ steps.version.outputs.version }}"
          body: |
            🎉 自动构建发布
  
            🏷 发布版本：${{ steps.version.outputs.version }}
            📦 内部版本：1.5 (WorldMagic)
            💻 构建环境：Temurin JDK 21
          
          # ⚠️ 关键修改：使用通配符匹配 target 目录下的任何 jar 包
          # 因为 pom.xml 生成的是 WorldMagic-1.5.jar，不是 world-magic.jar
          files: target/*.jar
          
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
